'use client'
import { motion, useReducedMotion, useScroll, useTransform } from 'framer-motion'
import { useRef } from 'react'
export function MugenOrb(){ const reduce=useReducedMotion(); const ref=useRef<HTMLDivElement>(null); const {scrollYProgress}=useScroll({target:ref,offset:['start end','end start']}); const x=useTransform(scrollYProgress,[0,1],[-24,24]); const y=useTransform(scrollYProgress,[0,1],[-16,16]); const rotate=useTransform(scrollYProgress,[0,1],[-8,8]); const scale=useTransform(scrollYProgress,[0,1],[0.98,1.02]); const svg=(<svg id="mugen-orb" viewBox="0 0 240 240" className="w-full max-w-[520px] h-[340px]" role="img" aria-label="Decorative orb"><defs><radialGradient id="g1" cx="50%" cy="50%" r="50%"><stop offset="0%" stopColor="#00C2D7" stopOpacity="0.95"/><stop offset="100%" stopColor="#00C2D7" stopOpacity="0.05"/></radialGradient><filter id="f1" x="-50%" y="-50%" width="200%" height="200%"><feGaussianBlur stdDeviation="8" result="b"/><feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge></filter></defs><circle cx="120" cy="120" r="100" fill="url(#g1)" filter="url(#f1)"/><path d="M40 120 C 80 40, 160 200, 200 120" fill="none" stroke="#00C2D7" strokeOpacity="0.6" strokeWidth="2"/></svg>); return <div ref={ref} className="rounded-2xl p-6 bg-black/20 flex items-center justify-center shadow-[0_0_0_1px_rgba(0,194,215,0.25),0_12px_60px_rgba(0,194,215,0.15)]">{reduce?svg:<motion.div style={{x,y,rotate,scale}}>{svg}</motion.div>}</div> }
