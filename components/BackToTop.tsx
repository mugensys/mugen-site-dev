'use client'
import { useEffect, useState } from 'react'; import { ArrowUp } from 'lucide-react'
export function BackToTop(){ const [show,setShow]=useState(false); useEffect(()=>{ const onScroll=()=>setShow(window.scrollY>600); onScroll(); window.addEventListener('scroll', onScroll, {passive:true}); return ()=>window.removeEventListener('scroll', onScroll)},[]); if(!show) return null; return (<button aria-label="Back to top" onClick={()=>window.scrollTo({top:0,behavior:'smooth'})} className="fixed bottom-6 right-6 h-11 px-4 rounded-2xl bg-[var(--brand)] text-black shadow-[0_10px_40px_rgba(243,106,16,0.25)]"><span className="inline-flex items-center gap-2"><ArrowUp className="h-4 w-4" /> Top</span></button>) }
