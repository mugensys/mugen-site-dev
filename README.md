# Mugen Systems — Mont‑Fort–inspired Static Site

Next.js 14 (App Router) static export; Tailwind; Framer Motion. COPY BLOCK rendered verbatim.

## Build
```bash
npm ci
npm run build   # -> ./out
```
